{
  "name": "bergensers-commons",
  "version": "0.0.0",
  "private": true,
  "description": "Test suite for the Bergensers Commons questionnaire (index.html). The app itself has no build step and no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
